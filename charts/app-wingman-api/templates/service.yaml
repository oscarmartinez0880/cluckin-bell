apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-wingman-api.fullname" . }}
  labels:
    {{- include "app-wingman-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-wingman-api.selectorLabels" . | nindent 4 }}
