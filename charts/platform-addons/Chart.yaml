apiVersion: v2
name: platform-addons
description: Umbrella Helm chart for Cluckin Bell platform add-ons (ALB Controller, ExternalDNS, cert-manager, kube-prometheus-stack, metrics-server)

type: application

# This is the chart version
version: 0.1.0

# This is the version number of the application being deployed
appVersion: "1.0.0"
